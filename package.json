{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Node.js on Docker",
  "author": "Matan Cohen",
  "main": "server.js",
  "scripts": {
    "start": "node --inspect ./src/index.js",
    "install:deps": "brew install skaffold",
    "dashboard:proxy": "kubectl proxy &",
    "kube:deploy": "kubectl apply -f kube"
  },
  "dependencies": {
    "express": "^4.16.1"
  }
}
